---
paths:
  - 'resources/views/**'
---

# Views

## Standalone Blade views, no build step
Views are complete HTML documents without layout inheritance or Blade components; CSS comes from CDN links. There is no Vite/npm build — do not add one.
